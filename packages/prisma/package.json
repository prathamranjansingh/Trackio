{
  "name": "@trackio/prisma",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "generate": "npx prisma generate --schema=./schema/schema.prisma",
    "push": "npx prisma db push --schema=./schema/schema.prisma",
    "migrate": "npx prisma migrate dev --schema=./schema/schema.prisma",
    "studio": "npx prisma studio --schema=./schema/schema.prisma",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/adapter-neon": "^6.10.0",
    "@prisma/client": "^6.15.0",
    "prisma": "^6.15.0",
    "ws": "^8.18.2"
  }
}
